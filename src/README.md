# Source layout

`App.jsx` is the composition root. UI components live under `components`, React orchestration under `hooks`, business rules under `domain`, demo data under `data`, and framework-agnostic helpers under `utils`.
